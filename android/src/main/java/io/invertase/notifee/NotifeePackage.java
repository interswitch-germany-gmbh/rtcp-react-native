package io.invertase.notifee;

import com.facebook.react.ReactPackage;

/**
 * Stub for React Native autolinking detection only.
 *
 * The CLI scans src/main/java/ for ReactPackage implementations to detect this library.
 * This directory is intentionally excluded from java.srcDirs in build.gradle so this
 * stub is never compiled — the real NotifeePackage is compiled from:
 *   notifee/packages/react-native/android/src/main/java/
 */
class NotifeePackage implements ReactPackage {}
